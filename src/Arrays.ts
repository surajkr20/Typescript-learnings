
let arr:number[] = [12,32,12,54,44];    // this array contains only numbers

for(let i=0; i<arr.length; i++){
    console.log(arr[i])
}

console.log(arr)

let Arr:Array<number | string> = ['one', 2, 'three', 4, 'five'] // similar to arr